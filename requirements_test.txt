homeassistant>=2024.6.0
pytest-homeassistant-custom-component
python-igps-ble>=0.1.0
bleak-retry-connector>=3.5.0

# Runtime deps of the HA bluetooth/usb components (installed by HA at runtime,
# needed explicitly for the test harness on macOS).
aiousbwatcher
habluetooth
bluetooth-adapters
bluetooth-data-tools
bleak
dbus-fast
pyserial
# Linux-only: HA's `usb` component (pulled in by the bluetooth stack) imports
# pyudev on Linux. Needed so the CI test harness can set up bluetooth_adapters.
pyudev; sys_platform == "linux"
